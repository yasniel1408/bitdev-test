import type { ReactAppOptions } from './react-app-options';
import type { Logger } from '@teambit/logger';
import { DependencyResolverMain } from '@teambit/dependency-resolver';
import { ApplicationType } from '@teambit/application';
import { EnvHandler } from '@teambit/envs';
import { ReactApp } from './react.application';
export declare type ReactAppTypeOptions = {
    name?: string;
};
export declare class ReactAppType implements ApplicationType<ReactAppOptions> {
    readonly name: string;
    private logger;
    private dependencyResolver;
    private pubsub;
    private workspacePath;
    constructor(name: string, logger: Logger, dependencyResolver: DependencyResolverMain, pubsub: any, workspacePath: string);
    createApp(options: ReactAppOptions): ReactApp;
    static from(appTypeOptions?: ReactAppTypeOptions): EnvHandler<ReactAppType>;
}
