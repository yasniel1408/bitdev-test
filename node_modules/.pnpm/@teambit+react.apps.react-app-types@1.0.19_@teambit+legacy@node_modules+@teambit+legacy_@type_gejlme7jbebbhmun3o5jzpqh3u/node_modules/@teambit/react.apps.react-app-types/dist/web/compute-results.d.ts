import { BundlerResult } from '@teambit/bundler';
import { ReactAppBuildResult } from './react-build-result';
declare type ResultsOptions = {
    publicDir: string;
    ssrPublicDir?: string;
};
export declare function computeResults(results: BundlerResult[], { publicDir, ssrPublicDir }: ResultsOptions): Promise<ReactAppBuildResult>;
export {};
