"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseAssets = exports.loadSsrApp = exports.createExpressSsr = void 0;
const ui_foundation_ui_pages_static_error_1 = require("@teambit/ui-foundation.ui.pages.static-error");
const react_rendering_ssr_1 = require("@teambit/react.rendering.ssr");
const express_1 = __importDefault(require("express"));
const fs = __importStar(require("fs-extra"));
const path_1 = require("path");
const url_join_1 = __importDefault(require("url-join"));
const webpack_app_config_1 = require("../webpack/webpack.app.config");
const MAGIC_FOLDER = 'public'; // idk where this is comping from
function createExpressSsr({ name, workdir, port, app, assets, logger, }) {
    const express = (0, express_1.default)();
    const publicFolder = (0, path_1.resolve)(workdir, (0, webpack_app_config_1.calcOutputPath)(name, 'browser'), MAGIC_FOLDER);
    express.use(webpack_app_config_1.PUBLIC_PATH, express_1.default.static(publicFolder));
    express.use((request, response, next) => {
        if (request.query._rendering !== 'client') {
            next();
            return;
        }
        response.sendFile((0, path_1.resolve)(workdir, (0, webpack_app_config_1.calcOutputPath)(name, 'browser'), MAGIC_FOLDER, 'index.html'));
    });
    express.use((request, response) => __awaiter(this, void 0, void 0, function* () {
        logger === null || logger === void 0 ? void 0 : logger.info(`[react.application] [ssr] handling "${request.url}"`);
        const browser = (0, react_rendering_ssr_1.browserFromExpress)(request, port);
        const session = { assets, browser, request, response };
        try {
            // the app itself controls the response
            yield app(session);
            logger === null || logger === void 0 ? void 0 : logger.console(`[react.application] [ssr] success "${request.url}"`);
        }
        catch (error) {
            logger === null || logger === void 0 ? void 0 : logger.consoleError(`[react.application] [ssr] error at "${request.url}"`, error);
            response.status(500).send((0, ui_foundation_ui_pages_static_error_1.serverError)());
        }
    }));
    return express;
}
exports.createExpressSsr = createExpressSsr;
function loadSsrApp(workdir, appName) {
    return __awaiter(this, void 0, void 0, function* () {
        const entryFile = (0, path_1.resolve)(workdir, (0, webpack_app_config_1.calcOutputPath)(appName, 'ssr'), MAGIC_FOLDER, webpack_app_config_1.SSR_ENTRY_FILE);
        if (!fs.existsSync(entryFile))
            throw new Error(`expected ssr bundle entry file at "${entryFile}"`);
        const entry = yield Promise.resolve().then(() => __importStar(require(entryFile)));
        const app = entry === null || entry === void 0 ? void 0 : entry.default;
        if (!app)
            throw new Error(`bundle entry file has no default export (at "${entryFile}")`);
        return app;
    });
}
exports.loadSsrApp = loadSsrApp;
function parseAssets(assets, publicPath = webpack_app_config_1.PUBLIC_PATH) {
    const deadAssets = assets.filter((x) => !x.name);
    if (deadAssets.length > 0)
        throw new Error('missing some build assets (maybe need to turn on cachedAssets, etc)');
    return {
        css: assets
            .map((x) => x.name)
            .filter((name) => name === null || name === void 0 ? void 0 : name.endsWith('.css'))
            .map((name) => (0, url_join_1.default)(publicPath, name)),
        js: assets
            .map((x) => x.name)
            .filter((name) => name === null || name === void 0 ? void 0 : name.endsWith('.js'))
            .map((name) => (0, url_join_1.default)(publicPath, name)),
    };
}
exports.parseAssets = parseAssets;
//# sourceMappingURL=ssr-express.js.map