import type { WebpackConfigMutator, WebpackConfigTransformContext } from '@teambit/webpack';
/**
 * force the browser to reload the favicon on every build.
 * @param filename the favicon filename. default to '/favicon.svg'.
 * It should start with '/'.
 */
export declare function faviconReload(filename?: string): (config: WebpackConfigMutator, context: WebpackConfigTransformContext) => WebpackConfigMutator;
