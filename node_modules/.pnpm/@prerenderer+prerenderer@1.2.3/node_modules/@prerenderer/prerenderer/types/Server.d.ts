import Prerenderer from './Prerenderer';
export type Stage = 'pre-static' | 'post-static' | 'pre-fallback' | 'post-fallback' | 'post-listen';
export default class Server {
    private prerenderer;
    private options;
    private readonly expressServer;
    private nativeServer;
    constructor(prerenderer: Prerenderer);
    getExpressServer(): import("express-serve-static-core").Express;
    getPrerenderer(): Prerenderer;
    initialize(): Promise<void>;
    destroy(): Promise<void>;
}
