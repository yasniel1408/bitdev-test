import { Express } from 'express';
import { ViteDevServer } from "vite";
export declare type CreateServerOptions = {
    publicDir: string;
    indexHtmlPath: string;
    serverPath: string;
    serverEntryFile: string;
    dev?: boolean;
    viteServer: ViteDevServer;
};
export declare function createSsrServer(options: CreateServerOptions): Promise<Express>;
