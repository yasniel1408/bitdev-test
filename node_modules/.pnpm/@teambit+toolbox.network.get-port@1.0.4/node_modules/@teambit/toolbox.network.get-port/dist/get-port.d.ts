export declare class Port {
    get(options: {
        port: number | Iterable<number>;
        usedPort?: number[];
    }): Promise<number>;
    private getAvailablePort;
    private makeRange;
    static getPort(from: number, to: number, usedPort?: number[]): Promise<number>;
    static getPortFromRange(range: number[] | number, usedPort?: number[]): Promise<number>;
}
