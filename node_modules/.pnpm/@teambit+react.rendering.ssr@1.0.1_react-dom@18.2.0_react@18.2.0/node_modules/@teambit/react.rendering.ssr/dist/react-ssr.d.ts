import type { RenderPlugin } from './render-plugins';
import type { BrowserRendererOptions } from './browser-renderer';
import type { ServerRendererOptions } from './server-renderer';
export declare type ReactSsrOptions = Partial<ServerRendererOptions & BrowserRendererOptions>;
export declare class Ssr {
    private plugins;
    private options?;
    constructor(plugins: RenderPlugin<any, any>[], options?: ReactSsrOptions);
    private browser;
    private server;
    renderServer: any;
    renderBrowser: any;
}
